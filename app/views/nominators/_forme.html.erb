<%= form_with model: nominator, url: user_update_nominator_path(@nominator), method: :patch, local: true do |form| %>
  <% if nominator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nominator.errors.count, "error") %> prohibited this nominator from being saved:</h2>

      <ul>
        <% nominator.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :university_id %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :nominator_email, "Email" %>
    <%= form.email_field :nominator_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
