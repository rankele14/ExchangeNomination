<%= form_with(model: authorized) do |form| %>
  <% if authorized.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authorized.errors.count, "error") %> prohibited this authorized from being saved:</h2>

      <ul>
        <% authorized.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :authorized_email %>
    <%= form.text_field :authorized_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
