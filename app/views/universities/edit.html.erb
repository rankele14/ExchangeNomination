<% content_for :title, "Edit University: " + @university.university_name%>

<h1>Editing University</h1>

<%= render 'form', university: @university %>

<%= link_to 'Show', @university %> |
<%= link_to 'Delete', delete_university_path(@university) %> | 
<%= link_to 'Back to Universities', universities_path %>
