<p id="notice"><%= notice %></p>

<h1>Representatives</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Title</th>
      <th>University</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @representatives.each do |representative| %>
      <tr>
        <td><%= representative.first_name %></td>
        <td><%= representative.last_name %></td>
        <td><%= representative.title %></td>
        <td><%= University.where(id: representative.university_id).last.university_name %></td>
		<td><%= representative.rep_email %></td>
        <td><%= link_to 'Show', representative %></td>
        <td><%= link_to 'Edit', edit_representative_path(representative) %></td>
        <td><%= link_to 'Destroy', representative, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Representative', new_representative_path %>
<br>
<br>
<%= link_to 'Back', students_path %>
